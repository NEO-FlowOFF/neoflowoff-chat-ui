# Skills — NØX

## Ativas

- **chat:** streaming SSE via `/api/chat` → Venice AI (Uncensored).
- **memory:** persistência soberana server-side via Redis (ioredis) com sincronização de sessão.
- **tactile-ui:** interface 3D com volume, rim lights e boot coreografado.
- **pwa-sovereign:** suporte offline e instalação home screen via service workers.

## Pendentes

- **context:** injeção de `ecosystem.json` no system prompt (RAG).
- **auth:** proteção da rota `/chat` via assinatura de wallet ou JWT.
- **sentiment:** análise de tom emocional para mudança dinâmica de UI.

---
> Nível de Operação: Beta v1.1.0
